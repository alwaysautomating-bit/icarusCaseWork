# Build Log

## 08-13-2026

### Session Goals

- Convert the Icarus Casework V1 specification and existing compiler artifacts into an executable end-to-end project plan.
- Install the minimum project operating system.

### Completed

- Reviewed the V1 product specification and existing second compiler pass.
- Confirmed that the project is kickoff-ready and already isolated in `C:\Projects\IcarusCasework`.
- Added canonical user workflows with terminal criteria, evidence, dispositions, and closure blockers.
- Added authority, epistemic, safety, and publication control boundaries.
- Added a gated end-to-end delivery plan and seeded durable project memory.

### Decisions

- Keep the evidence substrate authoritative and all hypotheses/simulations as projections.
- Require recorded human review for consequential state transitions and publication.
- Deliver through vertical slices with evidence-backed exit gates.
- Defer stack selection to an explicit Phase 0 architecture decision.

### Problems Found

- The repository has no Git history yet.
- No application scaffold, database, deployment, or automated tests exist yet.
- Exact persisted schema, citation locators, stack, hosting, and case-corpus acquisition remain implementation decisions.

### Files Created

- `_planning/compiled/USER-WORKFLOWS.md`
  Purpose: Defines the canonical end-user loop and explicit completion boundaries.
- `_planning/compiled/CONTROL-BOUNDARIES.md`
  Purpose: Defines authority, epistemic, safety, and publication rules.
- `_planning/compiled/DECISIONS.md`
  Purpose: Preserves compiler-level choices and their rationale.
- `PROJECT_08-13-2026.md`
  Purpose: Provides the current canonical project definition and phase.
- `BACKLOG_08-13-2026.md`
  Purpose: Provides the prioritized implementation horizon.
- `BUILD_LOG_08-13-2026.md`
  Purpose: Records this kickoff session and its evidence.
- `ONTOLOGY.md`
  Purpose: Establishes canonical load-bearing vocabulary.
- `DECISIONS_08-13-2026.md`
  Purpose: Records durable project and architectural decisions.

### Files Modified

- `_planning/compiled/IMPLEMENTATION-PLAN.md`
  Purpose: Expanded the initial slices into a complete build, pilot, and launch plan with measurable gates.

### Next Session

- Complete Phase 0 stack and schema decisions, then scaffold the app and implement Slice 1 with fixtures and tests.

## 08-13-2026 — Implementation Session

### Session Goals

- Decide the stack and persisted schema.
- Initialize a clean application repository.
- Build and verify the first source-linked vertical slice.

### Completed

- Selected and recorded the Next.js/PostgreSQL/PGlite/object-storage architecture.
- Created a clean Icarus Casework package identity, README, environment template, and Git repository.
- Implemented durable schema migration and checksum-addressed local artifact preservation.
- Implemented exact quote validation, attributed claim creation, human review, distinct event promotion, and source-linked timeline projection.
- Added responsive editorial UI, citation tests, type/lint checks, and production build verification.
- Exercised the full workflow in headless Chrome with no console errors or framework overlay.

### Decisions

- Keep local PGlite as a development adapter, not the deployed storage contract.
- Externalize PGlite from the Next.js server bundle.
- Keep authentication out of the local proof slice and explicitly prohibit public exposure until it exists.

### Problems Found

- PGlite must be a Next.js server external package; bundling it caused a runtime filesystem error.
- The bundled Playwright browser was absent, so verification used installed Chrome through Playwright.
- Authentication, multi-format locators, and deployed storage remain release blockers, not Slice 1 blockers.

### Files Created

- `src/db/schema.ts` and `src/db/migration.sql`
  Purpose: Define and apply the PostgreSQL-compatible evidence substrate.
- `src/lib/casework.ts` and `src/lib/citation.ts`
  Purpose: Implement transactional artifact, claim, review, event, and locator behavior.
- `src/app/actions.ts`, `src/app/page.tsx`, and `src/app/globals.css`
  Purpose: Deliver the complete researcher-visible vertical slice.
- `SCHEMA_08-13-2026.md`
  Purpose: Document persisted tables, relationships, and indexes.
- `.env.example`, `vitest.config.ts`, and `src/lib/citation.test.ts`
  Purpose: Provide reproducible configuration and regression verification.

### Files Modified

- `package.json`, `README.md`, `.gitignore`, `next.config.ts`, and `src/app/layout.tsx`
  Purpose: Clean application identity, scripts, local-data safety, runtime configuration, and metadata.
- `PROJECT_08-13-2026.md`, `BACKLOG_08-13-2026.md`, and `DECISIONS_08-13-2026.md`
  Purpose: Reconcile project phase, priorities, and architecture decisions.

### Next Session

- Begin Phase 2 with authentication design, multi-format source locators, provenance roles, entity resolution, and claim lineage.

## 08-13-2026 — Evidence Graph Session

### Session Goals

- Build the next coherent product increment across identity, provenance, lineage, contradiction, authority, and navigation.

### Completed

- Added durable entities and reversible source-specific aliases.
- Added explicit originator, publisher, custodian, and submitter provenance roles.
- Added typed claim lineage and a regression rule preventing repetition from counting as independent corroboration.
- Added unresolved contradictions linked to two distinct claims.
- Added actor-derived authorization checks at every Server Action and append-only audit events for every consequential mutation.
- Expanded the workspace with navigation, evidence graph forms, lineage/contradiction views, and an authority log.
- Exercised all new workflows in Chrome against the migrated durable database.

### Decisions

- Use a local owner actor adapter for the private local prototype.
- Require a provisioned external identity provider before any non-local deployment; no tenant or credentials were available in this session.
- Keep contradictions unresolved by default and prohibit silent closure.

### Problems Found

- The first automated browser assertion ran before the Server Action refresh was reflected locally; a fresh-page verification confirmed all records were persisted correctly.
- External authentication cannot be completed responsibly without a selected/provisioned tenant and secrets.

### Files Created

- `src/db/migration-0002-evidence-graph.sql`
  Purpose: Adds the evidence-graph and authority tables to existing databases.
- `src/lib/authority.ts`
  Purpose: Derives the case actor server-side for mutation authorization and audit attribution.
- `src/lib/evidence-graph.ts` and `src/lib/evidence-graph.test.ts`
  Purpose: Encodes and tests lineage integrity rules.

### Files Modified

- `src/db/schema.ts` and `src/lib/casework.ts`
  Purpose: Persist and operate the expanded evidence graph transactionally.
- `src/app/actions.ts`, `src/app/page.tsx`, and `src/app/globals.css`
  Purpose: Expose protected mutations and the expanded researcher workspace.
- `PROJECT_08-13-2026.md`, `BACKLOG_08-13-2026.md`, and `SCHEMA_08-13-2026.md`
  Purpose: Reconcile phase, next work, and persisted model.

### Next Session

- Build Phase 3 timeline lanes, uncertain intervals, contradiction dispositions, saved research views, and multi-format source locators.

## 08-13-2026 — Infrastructure Rules Session

### Session Goals

- Record binding infrastructure, authentication, and migration preferences before the next slice.

### Completed

- Added the project rules to repository-level agent instructions.
- Reconciled active project, architecture decisions, backlog, README, and schema policy with the preferences.
- Preserved PGlite and filesystem storage strictly as local-development adapters.

### Decisions

- Prefer Supabase or Neon for managed PostgreSQL.
- Prefer Blob-compatible object storage.
- Use Google, Apple, then magic links for authentication; prohibit Clerk.
- Require a documented reason before adopting Firebase.
- Require controlled migrations and prohibit Prisma `db push` as a production strategy.

### Problems Found

- The prior plan named Clerk as a possible deployment identity provider and generic managed PostgreSQL/S3 infrastructure. Those options are now superseded by the binding project preferences.

### Files Created

- None.

### Files Modified

- `AGENTS.md`
  Purpose: Makes infrastructure rules enforceable for future contributors and agents.
- `README.md`
  Purpose: Makes deployment and migration policy visible to developers.
- `DECISIONS_08-13-2026.md`
  Purpose: Records the owner-approved infrastructure decision and superseded alternatives.
- `PROJECT_08-13-2026.md`
  Purpose: Adds infrastructure constraints to the canonical project state.
- `BACKLOG_08-13-2026.md`
  Purpose: Reframes the next infrastructure work around Supabase/Neon, Blob storage, and the approved authentication order.
- `SCHEMA_08-13-2026.md`
  Purpose: Establishes the controlled migration policy.

### Next Session

- Design the next slice against Supabase/Neon and Blob-compatible adapters, with Google/Apple/magic-link authentication and controlled deployment migrations.

## 08-13-2026 — Phase 3 Reconstruction Slice

### Session Goals

- Start the next researcher reconstruction slice under the approved infrastructure rules.

### Completed

- Selected Supabase for V1 PostgreSQL and authentication, with Neon retained as a documented fallback.
- Installed and pinned Supabase CLI `2.113.0`, initialized the project, and generated the bootstrap migration through the CLI.
- Added an ownership-aware Supabase schema with explicit authenticated grants, RLS, private authorization helpers, and automatic owner membership.
- Added incident anchors, 90-day/30-day/incident-window projections, saved research views, event interval/uncertainty capture, and terminal contradiction dispositions.
- Added nine regression tests, including a from-zero application of the Supabase deployment migration.
- Verified the live saved-view and contradiction-disposition workflows in Chrome.

### Decisions

- Use Supabase Auth in the approved order: Google, Apple, then magic links.
- Use Vercel Blob or equivalent for deployed objects; the adapter remains unimplemented.
- Keep PGlite as a local adapter and application authorization as the primary control.

### Problems Found

- Docker is not installed, so the full local Supabase stack, `db reset`, and `migration list --local` could not run.
- Cloud project credentials, OAuth provider credentials, and Blob credentials are not available.
- Saved views persist their boundaries but do not yet activate UI filtering.

### Files Created

- `supabase/config.toml` and `supabase/migrations/20260813090819_bootstrap_icarus_casework.sql`
  Purpose: Establish the controlled Supabase development and deployment contract.
- `src/db/migration-0003-reconstruction-views.sql` and `src/db/migration-0004-incident-anchor.sql`
  Purpose: Evolve existing local databases through ordered migrations.
- `src/lib/timeline.ts` and `src/lib/timeline.test.ts`
  Purpose: Provide deterministic nested-window projection rules.
- `src/db/supabase-migration.test.ts`
  Purpose: Verify the deployment migration applies from zero.
- `DEPLOYMENT_08-13-2026.md`
  Purpose: Define infrastructure, security, migration, and deployment gates.

### Files Modified

- `src/db/schema.ts`, `src/lib/casework.ts`, and `src/app/actions.ts`
  Purpose: Persist interval, saved-view, and terminal-disposition behavior.
- `src/app/page.tsx` and `src/app/globals.css`
  Purpose: Add synchronized timeline lanes, saved views, and disposition controls.
- `.env.example`, `package.json`, and `pnpm-lock.yaml`
  Purpose: Add safe provider configuration and a pinned Supabase CLI.
- `PROJECT_08-13-2026.md`, `BACKLOG_08-13-2026.md`, `SCHEMA_08-13-2026.md`, `ONTOLOGY.md`, `DECISIONS_08-13-2026.md`, and `README.md`
  Purpose: Reconcile project state, terminology, infrastructure, verification, and remaining debt.

### Next Session

- Add active saved-view filtering, multi-format source locators, and the Blob-compatible object-storage adapter; provision Supabase only when project ownership and credentials are available.

## Session — Active research views and storage-ready citations

### Accomplished

- Installed Docker Desktop `4.86.0`; verified Docker CLI `29.7.2`. Docker reported that WSL is absent, and an elevated WSL/Virtual Machine Platform enablement window was opened. A Windows restart remains required before any container-backed Supabase result can be claimed.
- Activated saved research views through URL-addressable filters. A selected view narrows timeline lanes and can exclude unresolved contradictions.
- Added typed citation coordinates for characters, pages, timestamps, spreadsheet ranges, and image regions while retaining exact extracted-text offsets.
- Added an immutable object-storage boundary with filesystem development storage and private Vercel Blob deployment storage; pinned `@vercel/blob` `2.8.0`.
- Verified HTTP 200 rendering, new controls, and absence of the Next.js error overlay. The skill-provided browser CLI was unavailable and the in-app control connection timed out, so the runtime check used direct rendered HTML evidence.
- Passed ESLint, TypeScript, 12 Vitest tests across 6 files, and the Next.js production build.

### Files Created

- `src/lib/source-locator.ts` and `src/lib/source-locator.test.ts` — typed locator construction, formatting, and validation.
- `src/lib/object-storage.ts` and `src/lib/object-storage.test.ts` — immutable local/private-Blob storage adapters and local regression coverage.

### Files Modified

- `src/lib/casework.ts`, `src/db/schema.ts`, `src/app/page.tsx`, and `src/app/globals.css` — integrate multi-format citations, storage selection, and active saved views.
- `package.json` and `pnpm-lock.yaml` — pin Vercel Blob SDK.
- Managed project documents — reconcile state, decisions, ontology, schema, deployment evidence, and remaining debt.

### Technical Debt

- Finish the elevated WSL feature enablement, restart Windows, start Docker Desktop, then run the local Supabase migration/RLS replay (risk: medium; deployment gate).
- Provision and verify a private Blob store, including authenticated delivery and deletion lifecycle (risk: high; deployment gate).
- Replace pasted extracted-text ingestion with controlled binary uploads and source extraction; conditionally render locator fields (risk: medium; usability and fidelity).

### Next Session

- Build authenticated binary source upload and private retrieval, then run Docker-backed Supabase migration/RLS verification once the engine is available.
